# Use a separate database for development. Do not commit the real .env file.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/round_robin?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="15m"
CLIENT_ORIGIN="http://localhost:5173"
PORT=3000
